{
  "name": "backend",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "migrate": "bun run ./migrate.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.4",
    "hono": "^4.7.11",
    "jose": "^6.0.11",
    "stripe": "^18.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.30.0"
  }
}
